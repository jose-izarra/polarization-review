[project]
name = "scrape"
version = "0.1.0"
description = "Package for scraping recent data off the internet"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-api-python-client>=2.189.0",
    "praw>=7.8.1",
    "prawcore>=2.4.0",
]
